defmodule Matrex.Controllers.Client.R0.UserFilter do

  use Matrex.Web, :controller


  def get(conn, %{user_id: user_id, filter_id: filter_id}) do

  end

  def post(conn, %{user_id: user_id}) do
    with {:ok, args} <- parse_args(conn.body_params) do
      :ok
    end

  end


  defp parse_args(params) do
    args = %{}
  end



end
